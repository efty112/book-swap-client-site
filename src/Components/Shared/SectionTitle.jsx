const SectionTitle = ({heading}) => {
    return (
        <div>
            <h1 className="text-5xl text-center font-semibold my-16">{heading}</h1>
        </div>
    );
};

export default SectionTitle;